AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >
  Backend com HTTP API, Lambda e Cognito Auth

Parameters:
  MyUserPoolId:
    Type: String
    Description: ID do user pool do Cognito
  
  MyAppClientId:
    Type: String
    Description: ID do client app do Cognito
  
  SupabaseUrl:
    Type: String
    Description: URL do projeto Supabase
    NoEcho: true
  
  SupabaseKey:
    Type: String
    Description: Chave de API do Supabase (Anon ou Service)
    NoEcho: true

  TmdbApiKey:
    Type: String
    Description: Chave de API do TMDB
    NoEcho: true

  SteamgriddbApiKey:
    Type: String
    Description: Chave de API do SteamGridDB
    NoEcho: true
  
  MALClientId:
    Type: String
    Description: ClientID do MyAnimeList
    NoEcho: true
  
  MALClientSecret:
    Type: String
    Description: Client Secret do MyAnimeList
    NoEcho: true
  

Globals:
  Function:
    Timeout: 180
    MemorySize: 1024
    Runtime: python3.13
    Architectures:
      - arm64
    Environment:
      Variables:
        SUPABASE_URL: !Ref SupabaseUrl
        SUPABASE_KEY: !Ref SupabaseKey
        TABLE_NAME: !Ref TabelaDados
        TMDB_API_KEY: !Ref TmdbApiKey
        STEAMGRIDDB_API_KEY: !Ref SteamgriddbApiKey
        MAL_CLIENT_ID: !Ref MALClientId
        MAL_CLIENT_SECRET: !Ref MALClientSecret

Resources:
  TabelaDados:
    Type: AWS::DynamoDB::Table
    Properties:
      AttributeDefinitions:
        - AttributeName: user_id
          AttributeType: S
        - AttributeName: sk
          AttributeType: S
      KeySchema:
        - AttributeName: user_id
          KeyType: HASH
        - AttributeName: sk
          KeyType: RANGE
      BillingMode: PAY_PER_REQUEST
      SSESpecification:
        SSEEnabled: true

  TabelaDadosParam:
    Type: AWS::SSM::Parameter
    Properties:
      Name: /mylib/prod/tabela_dados
      Type: String
      Value: !Ref TabelaDados
      Description: "Nome dinâmico da tabela gerado pelo CloudFormation"

  MyHttpApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: prod
      CorsConfiguration:
        AllowMethods:
          - GET
          - POST
          - PUT
          - DELETE
          - PATCH
        AllowHeaders:
          - "*"
        AllowOrigins:
          - "*"
      
      Auth:
        Authorizers:
          CognitoAuthorizer:
            IdentitySource: "$request.header.Authorization"
            JwtConfiguration:
              issuer: !Sub "https://cognito-idp.${AWS::Region}.amazonaws.com/${MyUserPoolId}"
              audience:
                - !Ref MyAppClientId
        DefaultAuthorizer: CognitoAuthorizer
  
  CommonLayer:
    Type: AWS::Serverless::LayerVersion
    Properties:
      LayerName: mylib-common-layer
      Description: Utilitarios (Wrappers, Erros, Validadores)
      ContentUri: src/layers/common_layer/
      CompatibleRuntimes:
        - python3.13
      RetentionPolicy: Retain

  HeartbeatFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/functions/system
      Handler: heartbeat.lambda_handler
      Events:
        RootRoute:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /
            Method: GET
            Auth:
              Authorizer: NONE
  
  SearchFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/functions/system
      Handler: search.lambda_handler
      Layers:
        - !Ref CommonLayer
      Events:
        RootRoute:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /search
            Method: GET
            Auth:
              Authorizer: NONE

  CatalogAddItemFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/functions/catalog/
      Handler: add_item.lambda_handler
      Layers:
        - !Ref CommonLayer
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref TabelaDados
      Events:
        RootRoute:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /catalog
            Method: POST
    
  CatalogListItemsFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/functions/catalog/
      Handler: list_items.lambda_handler
      Layers:
        - !Ref CommonLayer
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref TabelaDados
      Events:
        RootRoute:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /catalog
            Method: GET

  CatalogDeleteItemFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/functions/catalog/
      Handler: delete_item.lambda_handler
      Layers:
        - !Ref CommonLayer
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref TabelaDados
      Events:
        RootRoute:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /catalog
            Method: DELETE
    
  CatalogUpdateItemFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/functions/catalog/
      Handler: update_item.lambda_handler
      Layers:
        - !Ref CommonLayer
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref TabelaDados
      Events:
        RootRoute:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /catalog
            Method: PATCH
  
  CatalogSyncMALFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/functions/catalog/
      Handler: sync_mal.lambda_handler
      Layers:
        - !Ref CommonLayer
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref TabelaDados
      Events:
        RootRoute:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /catalog/sync-mal
            Method: POST

  RecommendationsGetItemFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/functions/recommendations/
      Handler: get_item_recommendations.lambda_handler
      Layers:
        - !Ref CommonLayer
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref TabelaDados
      Events:
        RootRoute:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /recommendations/item
            Method: GET
  
  RecommendationsGetUserFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: src/functions/recommendations/
      Handler: get_user_recommendations.lambda_handler
      Layers:
        - !Ref CommonLayer
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref TabelaDados
      Events:
        RootRoute:
          Type: HttpApi
          Properties:
            ApiId: !Ref MyHttpApi
            Path: /recommendations/user
            Method: GET


Outputs:
  ApiEndpoint:
    Description: "URL da API HTTP"
    Value: !Sub "https://${MyHttpApi}.execute-api.${AWS::Region}.amazonaws.com/prod"